import React from 'react'
import { createRoot } from 'react-dom/client'
const mainElement = document.getElementsByTagName('main')

const root = createRoot(mainElement[0])
root.render(<div>tssssest</div>)
